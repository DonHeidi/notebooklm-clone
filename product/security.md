# Security register

> **Status:** Living document, started 2026-08-18. One entry per known
> security concern: what it is, what mitigates it today, whether the residual
> risk is accepted for the prototype, and the trigger that ends the
> acceptance. Review sessions append here instead of burying findings in PR
> comments. Scope-doc anchors: NF-07 (security), NF-17 (safety/prompt
> injection).
>
> **Acceptance model:** "Accepted (prototype)" means: single-tenant-ish demo,
> authenticated users only, private repo, no untrusted public traffic. Every
> acceptance below names the trigger that revokes it — most commonly "before
> public exposure" = opening signups/URLs to people we don't know.

## Open — accepted for the prototype

| ID | Area | Concern | Current mitigation | Hardening trigger / plan |
| --- | --- | --- | --- | --- |
| SEC-1 | Ingestion (A3) | **SSRF via URL sources.** The guard blocks loopback/private/link-local hostnames but checks only the *original* hostname while `redirect: "follow"` is on — a public URL 30x-ing to a private address gets fetched. No DNS resolution check (rebinding). | Hostname blocklist regex; http(s)-only; 15s timeout; content-type check; authenticated users only; container has no privileged internal network neighbors today. | Before public exposure, or before the container gets internal neighbors (e.g. VPC services): resolve DNS and re-check each redirect hop (undici custom dispatcher or manual redirect loop), or route fetches through an egress proxy. |
| SEC-2 | Ingestion (A3) | **Oversized/hostile files cost parse CPU.** The 20 MB cap is enforced at upload (client + bucket + server), but word-count limits are enforced only *after* parsing — a crafted PDF can spike CPU/memory in-process (mammoth-class parser bombs noted in feasibility research too). | 20 MB hard cap; in-process ingestion means a crash affects one request, status goes `failed`; authenticated users only. | D-2 stage 2 (Serverless Jobs) isolates parsing in a disposable container with its own CPU/memory limits — the structural fix. Until then, acceptable. |
| SEC-3 | Ingestion / chat (A3→A4) | **Prompt injection via source content (NF-17).** Parsed web pages and documents are untrusted input fed into A4's chat prompts. | **A4 (2026-08-18) implements the treat-chunks-as-data contract:** retrieved chunks enter the prompt only inside delimited `<<<BEGIN/END SOURCE [n]>>>` blocks (delimiter sequences are stripped from chunk text so a source cannot fake a boundary); the system prompt declares quoted material to be data, never instructions; the chat model has **no tools** and no authority beyond emitting answer text; `[n]` markers are mapped server-side against the retrieved set only (invented markers are dropped). See `apps/webapp/src/server/ai/grounding.ts`. | Residual risk: instruction-following leakage is never fully preventable with prompting alone — a hostile source can still bias the *content* of answers about itself. Acceptable while sources are user-chosen. Re-review if chat ever gains tools (then injected text could trigger actions) or shared notebooks land (hostile sources chosen by someone else). |
| SEC-5 | AuthZ (A1/A2) | **RLS is defense-in-depth, not the primary guard.** The app connects via the pooler as `postgres`; the service-role storage client bypasses RLS entirely. Authorization correctness rests on the app layer (every repository/service call owner-scoped). | Consistent `ownerId`-from-JWT pattern (`requireUser()`); owner-scoping tested (incl. cross-user 404s); RLS policies on all 7 tables + storage objects for any PostgREST/Realtime path. | Standing review item: every new repository/service method must take and apply `ownerId`. If Realtime/PostgREST client access is ever added, RLS becomes load-bearing — re-audit policies then. |
| SEC-6 | Secrets | **Service-role key + Scaleway/Azure keys are high-privilege.** `SUPABASE_SERVICE_ROLE_KEY` bypasses RLS; SCW keys control infra; keys live in dev `.env.local` files and GitHub Actions secrets (B2). **Inventory grew in B3 (2026-08-18):** the *hosted* service-role key + DB password (`.env.local` as `TF_VAR_*`), and a Supabase account access token (system keyring, written by `supabase login` — controls the whole Supabase org). B3 also means secret env values are stored in the **Terraform state** (`secret_environment_variables`) in the private, versioned `marginalia-tfstate` bucket — readable by anyone holding SCW keys. Nuance: the *anon* key is publishable by design (RLS enforces access); it is inlined into the client bundle on purpose. | varlock schema keeps values out of the repo; Proton Pass is the store; keys never printed in sessions/PRs (convention enforced in briefs); `.env*` gitignored; tfstate bucket private + versioned; no secrets in Terraform outputs (checked in B3, re-checked in B5). **B5 (2026-08-18):** the `supabase_apikeys` data source persists the hosted anon + service-role keys in Terraform state (same accepted state-resident class as B3's container secrets; the service-role key was already there via `secret_environment_variables`). The hosted DB password is *not* in state (unreadable at import; `ignore_changes` — it would land there only on recreation). The account access token is required at every plan/apply but stays keyring-only with ephemeral per-command injection — never in tfvars, `.env.local`, state, outputs, or Proton Pass (see `infrastructure/AGENTS.md`). | Rotate any key that ever appears in a log/transcript. Later: scoped IAM policies per key (Scaleway IAM application per use); consider Scaleway Secret Manager instead of state-embedded container secrets before public exposure. |
| SEC-7 | Abuse limits (NF-15) | **No rate limiting on actions/chat.** Guard limits exist (20 MB, 200k words, 50 sources/notebook) but nothing limits request *rate* — an authenticated user can hammer ingestion/embedding (token spend). | Closed signup circle during prototype; Scaleway org-level rate limits backstop token spend. **B3 (2026-08-18): hosted Supabase auth adds real per-IP limits** (30 sign-in/sign-ups per 5 min, 30 OTP verifications per 5 min, 150 token refreshes per 5 min; built-in mailer hard-capped at 2 emails/hour) — config-as-code in `supabase/config.toml` `[remotes.demo]`. **A6 (2026-08-18): per-user quotas cap total daily spend** — 20 notebooks/user, 50 chat messages/notebook/day, 10 audio overviews/user/day, enforced in the service layer via repository counts (`src/server/services/quota.ts`). Quotas bound the day's volume; request *rate* within those bounds is still unlimited. | Before public exposure: per-user rate limits on server actions and (critically) A4 chat + D2 audio generation; quota columns exist conceptually in scope SF-11. |
| SEC-8 | Uploads | **No malware scanning; filenames stored raw.** Uploaded files are stored and parsed but never scanned (NF-07 lists scanning); original filenames land in path segment 3 and DB titles. | Private bucket; files only ever parsed server-side, never served back for download to other users; path prefix validated. | Only relevant with sharing (SF-05) or file re-download features — add scanning + filename sanitization then. |
| SEC-9 | Web (A2) | **No CSRF-hardening beyond framework defaults; no security headers set.** Next server actions have same-origin protections built in, but we set no explicit CSP/HSTS/frame headers. | Framework defaults (Server Actions origin checks); TLS via platform endpoints. | B3/demo-hardening: add security headers (CSP tuned for the app, HSTS, X-Frame-Options) in next.config or at the edge. |
| SEC-10 | Storage abuse (B3) | **Storage as a free file locker — including for illegal content.** Signup is self-service and scriptable; storage RLS grants any authenticated user INSERT/SELECT/DELETE in BOTH buckets under their own `<uid>/…` prefix with no MIME restriction and no link to notebook/source rows — one throwaway account can upload/download arbitrary 20 MB objects via the storage REST API, outside all app-level guards (verified against hosted policies, 2026-08-18). Two impact tiers: (a) resource exhaustion — Free tier cannot bill, ~1 GB cap ≈ 50 max-size objects, then legitimate uploads fail; (b) **content liability (owner-flagged 2026-08-18): a stranger storing illegal material (e.g. CSAM) on a project registered to the owner creates legal exposure and takedown/reporting obligations regardless of caps, privacy of buckets, or tier — and pseudonymous accounts make attribution weak.** | Closed signup circle during the prototype is the load-bearing control: nobody unknown can obtain an account. Per-user prefix isolation keeps other users' data untouched either way. | **Hard launch blocker — owner decision 2026-08-18: must be fixed before operating as a real product; deferral is accepted for the closed-circle prototype only.** Required before opening signups: CAPTCHA/verified signup (with SEC-7), per-user storage quota, MIME/content restrictions + scanning (with SEC-8), orphan-object cleanup, and an abuse path (identify uploader, remove content, ban account). Independent quick win for any session owning migrations: **drop the user INSERT/DELETE policies on `artifacts`** — only the service role writes artifacts (server-side generation + signed-URL playback), so those policies are unused writable surface. |

## Closed / resolved

| ID | Area | Concern | Resolution |
| --- | --- | --- | --- |
| SEC-4 | Platform (B1/B2) | **Spike route publicly reachable.** `/api/spike-stream` on the deployed container (B1's image) was unauthenticated on a public URL, proxying to the Generative-APIs account (token cost abuse). Largely resolved by PR #13 (B2, 2026-08-18): the deployed container runs current main, so the route sat behind A2's auth proxy (verified by curl at review). | **Closed by A4 (PR: feat/grounded-chat, 2026-08-18): the route is deleted.** The real chat route replaces it under `requireUser()`-style auth (401 without a verified JWT). |

## Process

- **Adding entries:** any session or review that surfaces a concern appends a
  row (next SEC-n) in its PR; the foreman keeps this file consistent.
- **Closing entries:** move the row to Closed with the PR number that
  resolved it. Do not delete rows — the register is also the audit trail of
  what was consciously accepted and when.
- **Standing review checks** (foreman, every PR): new repository/service
  methods owner-scoped (SEC-5); no secret values in code, logs, or PR text
  (SEC-6); new outbound fetches go through the SSRF-guarded path (SEC-1);
  anything touching A4 prompting respects SEC-3.
